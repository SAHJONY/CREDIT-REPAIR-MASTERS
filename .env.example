# Server-side only. Never prefix these with NEXT_PUBLIC_.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6
CREDIT_OS_API_TOKEN=
CREDIT_OS_ORGANIZATION_ID=

# Production persistence/auth providers are intentionally not active in demo-safe mode.
DATABASE_URL=
AUTH_SECRET=

# Production infrastructure mode. Vercel production also enforces operator auth fail-closed.
APP_ENV=demo
EVIDENCE_VAULT_PROVIDER=
